{
  "name": "amd-discussions-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for per-study discussion boards",
  "main": "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "db:migrate:local": "wrangler d1 migrations apply amd-discussions --local",
    "db:migrate:remote": "wrangler d1 migrations apply amd-discussions --remote"
  },
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "itty-router": "^5.0.24"
  },
  "devDependencies": {
    "wrangler": "^4.104.0"
  }
}
