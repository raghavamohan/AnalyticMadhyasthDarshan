# Website improvement plan

Initial review: 5 September 2026. Status reviewed: **9 September 2026** (website-first / no native-app decision), previously **7 September 2026** against `master` / `origin/master` at `4c99dc1` (through PR #409). Scope: the public catalog, study readers, My Submissions, discussions, document publishing, and retrieval architecture.

## Recommendation

**Phases 1–5 are merged.** The reader now has contents, resume, subsection bookmarks, passage search, source previews, private notes, offline saving and selected-text audio. Contributor draft recovery and submission reliability are also delivered. The next work should make these features easier to discover and use together, then validate them in sustained reading and contribution tasks. Keep the static publishing architecture and existing lexical search; a vector database remains an optional evaluated extension.

**Do not build native Android or iPhone store applications.** The website is the product. Public catalog APIs, HTML/PDF/Markdown, discussion boards, and the GitHub-backed submission portal already cover reading, commenting, proposing studies, and filing corrections in the browser. Write APIs are cookie, Origin, and Turnstile contracts for humans on this origin (`auth.md`); they are not a mobile-client platform. A store app would either wrap the site (likely rejected, and still two listings to maintain) or reimplement the reader and lag glossary, maths, diagrams, notes, search, and offline saving. Put that effort into an installable website, a visible change feed, discussion reply mail, and the remaining UX items below.

This update reconciles merged PRs, deployment/check results, current source, dependency audits and sampled public-site Chromium checks, plus the 9 September product decision. It is not a penetration test, a complete accessibility audit or an authenticated production submission test. Historical implementation records below retain their original validation results; the remaining-work table governs current priorities.

## Current status and evidence

- **Merged and deployed:** [#396](https://github.com/raghavamohan/AnalyticMadhyasthDarshan/pull/396) delivered Phase 1, [#400](https://github.com/raghavamohan/AnalyticMadhyasthDarshan/pull/400) Phase 2, [#401](https://github.com/raghavamohan/AnalyticMadhyasthDarshan/pull/401) Phase 3, [#402](https://github.com/raghavamohan/AnalyticMadhyasthDarshan/pull/402) Phase 5, and [#403](https://github.com/raghavamohan/AnalyticMadhyasthDarshan/pull/403) Phase 4. The latest static deployment, index verification and generated-PDF publishing runs succeeded on `4c99dc1`. No open PR remained at review time. Deployment success does not establish that a real authenticated submission has been tested.
- **Navigation follow-ups are complete:** [#404](https://github.com/raghavamohan/AnalyticMadhyasthDarshan/pull/404) added Search and My Notes to catalog navigation and removed the intrusive catalog notice; [#405](https://github.com/raghavamohan/AnalyticMadhyasthDarshan/pull/405) fixed Next at initial sections; [#406](https://github.com/raghavamohan/AnalyticMadhyasthDarshan/pull/406) made bookmark targets explicit and sensitive to the nearest subsection.
- **Reading-flow follow-ups are complete:** [#407](https://github.com/raghavamohan/AnalyticMadhyasthDarshan/pull/407) compacted the toolbar, prefills reader correction context, restores the originating catalog card/Start here stage, makes section flashes consistent, and centralizes the shared approach instead of repeating it in every study. [#408](https://github.com/raghavamohan/AnalyticMadhyasthDarshan/pull/408) fixed Android Listen; the user subsequently confirmed it works on their phone. [#409](https://github.com/raghavamohan/AnalyticMadhyasthDarshan/pull/409) unified toolbar styling and kept the mobile drawer's toggle reachable. These are closed implementation items, not work to repeat.
- **Publishing efficiency is delivered:** [#397](https://github.com/raghavamohan/AnalyticMadhyasthDarshan/pull/397) reuses verified PDF builds when their inputs are unchanged. Continue checking build selection when shared generators change; do not interpret a triggered verification workflow alone as redundant PDF publication.
- **Live sample on 7 September:** catalog navigation exposes Search, My Notes and My Submissions; `satta` search returned 116 passages across 31 searched documents; the notebook and signed-out portal loaded; the ontology reader's toggle closed on desktop and at a 390-pixel viewport; its discussion loaded its empty state. This was a public-page spot check, not a fresh end-to-end test of every stored-data operation.
- **Validation still has limits:** the latest toolbar PR passed all 41 enforced local suites and its required CI checks. Existing browser checks mostly use Chromium with mobile viewport sizes. Physical Android audio now has one user confirmation; Safari/iOS, Firefox, TalkBack/VoiceOver/NVDA, storage eviction and long-session usability remain unverified across a supported-device matrix. No fresh post-rollout field-performance baseline is saved in this repository.
- **Security maintenance is still open:** audits rerun on 7 September report zero known package vulnerabilities for both API Workers and four high-severity entries in Scripts, all tracing to the recorded `extract-zip` installer advisory. This does not establish that either Worker is free of application vulnerabilities. Discussion session revocation and abuse/retention follow-ups remain separate work.
- **Website-first gap (9 September):** readers who want an “app” have apple-touch and app-icon assets but no Web App Manifest, no Add to Home Screen guidance, and no catalog UI for the existing JSON Feed (`Studies/feed.json`). Discussion boards have no reply mail or report control. Corrections still require a GitHub account. Those jobs belong on the site (SITE-01, SITE-02, DIS-01, FBK-01), not in store clients. The public API plan records the same constraint.

## Original baseline and findings — 5 September

The following describes the pre-improvement baseline. Its defects are historical unless explicitly retained in the remaining-work table; its traffic and transfer measurements must not be presented as current performance.

- The catalog contains 27 entries: 8 released, 5 drafts, and 14 ongoing. The 13 available studies contain approximately 175,000 whitespace-separated words, including references and markup. The largest documents are about 34,000 words each.
- All 26 catalog HTML/PDF destinations returned HTTP 200 in the sampled link check. Sampled HTML was Brotli-compressed and served from Cloudflare cache. Approximate transferred HTML sizes: catalog 28 KB, submission portal 29 KB, ontology reader 100 KB; decoded sizes are substantially larger.
- HSTS, CSP, frame restrictions, and MIME sniffing protection are present on the sampled live site. These are useful defenses, but do not replace safe rendering or request authorization.
- The stored August 30 RUM baseline covers only 45 page views, including 34 catalog views. It reports catalog p75 LCP of 2.6 seconds and TTFB of 1.296 seconds. The reported CLS of 1.0 and INP of zero need investigation; zero INP in this sample does not establish good responsiveness. Collect fresh, segmented data before drawing conclusions.
- GitHub OAuth used a constant authorization state and did not compare the callback state. Its session helper could place the GitHub access token in a signed, readable cookie when KV was absent.
- Cookie-authenticated write routes relied on CORS/SameSite without a common request-origin and JSON enforcement boundary. Personalized API responses lacked an explicit private/no-store policy.
- Discussion magic tokens were selected and then marked used in separate statements, allowing simultaneous consumption. Tokens were stored in directly usable form.
- Markdown rendering accepted raw active HTML; Mermaid used loose security mode. Generated page metadata also needs protection against closing-script text. PDF rendering should deny arbitrary network and local-file access.
- The reader’s Next section control is disabled above the first section. Only top-level headings receive sticky-toolbar anchor offsets. Explicit light mode can inherit the browser’s dark canvas, and narrow screens can overflow. Catalog date sorting passes human-formatted IST strings directly to Date.parse.
- My Submissions already has autosave, previews, revision workflows, and filters. Its single local draft key and limited preview fidelity remain substantial future reliability work.

## Delivery phases

Phases 1–5 are implemented and merged; Phase 5 preceded Phase 4 at the maintainer's request. Feature delivery and completion of real-user evaluation are separate. Phase 6 has not started and is not a prerequisite for the next UX work. Phase 7 is the website-first substitute for a store app; SITE-01 and SITE-02 can ship with the next UX delivery. DIS-01 waits for OPS-01.

| Phase | Status | Delivered scope | Remaining follow-through |
| --- | --- | --- | --- |
| 1 — Defects and security boundaries | Merged #396 | OAuth state/PKCE, server-side tokens, request/cache guards, atomic magic links, safe document rendering, navigation/theme/reflow and date fixes | Production authentication checks, dependency upgrade and discussion hardening: UX-05, OPS-01 |
| 2 — Reader essentials | Merged #400; fixes #405–409 | Contents, current section, display preferences, precise resume, named subsection bookmarks, consistent compact toolbar and mobile drawer | Journey consistency, bookmark backup and broader usability/accessibility validation: UX-01, UX-03, UX-04 |
| 3 — Find and verify | Merged #401 | In-study and collection passage search, stable passage links, bibliography previews and enlarged figures/tables | Ranking/grouping/aliases and deeper source discovery: UX-02, UX-07 |
| 4 — Contributor reliability | Merged #403 | Independent drafts, save/recovery state, source comparison, isolated rich preview, reviewer feedback, retries and submission receipts | Production workflow validation and discussion composer recovery: UX-05 |
| 5 — Deeper study tools | Merged #402; fixes #408–409 | Highlights, private notes, Markdown/JSON export/restore, offline copies and selected-text device audio | Notebook findability, backup/offline clarity and real-device evaluation: UX-03, UX-04 |
| 6 — Semantic retrieval pilot | Optional; not started | No implementation yet | Demonstrate unmet retrieval needs, curate/evaluate a public corpus, then decide whether hybrid retrieval merits deployment |
| 7 — Installable site and follow-through | Planned; not started | None yet | Home-screen install, visible catalog updates, discussion reply mail after hardening: SITE-01, SITE-02, DIS-01. Not a native application. |

## Remaining work, in priority order

P1 means the next quality/improvement cycle; P2 means a subsequent extension. These are planned items, not newly implemented behavior or claims of confirmed production failures. Estimates are active engineering effort for one developer, excluding user recruitment and field-observation time.

| ID / priority | Evidence or remaining need | Recommended work and user benefit | Completion evidence / estimate |
| --- | --- | --- | --- |
| UX-01 / P1 — Consistent navigation and return context | Search and My Notes each have only All studies as their site-level navigation; the portal and discussions use different headers. The reader's catalog return was fixed, but a discussion's Read the study link has no passage context. | Establish compact shared navigation, theme behavior, labels and focus styles across catalog, search, notebook, portal and discussions. Preserve query, filters and passage context through reader → discussion/source → reader and search → result → Back. Keep the reader toolbar compact. Use accessible labels and optional help, not another permanent explanatory banner. Extend the reader's correction-prefill contract to discussion entry points. | Keyboard and touch users can move between these pages and return to the same result/passage without reconstructing their context. Check Start here and catalog-card return as regressions. 2–3 days. |
| UX-02 / P1 — More useful literal search | Search works, but `Assets/reader/search.js` sorts results by document title. A long early-alphabet document can dominate the first batch. Some document labels come from generic headings, such as “PRESENTER’S COMPANION”; the current language menu contains only English. | Rank exact phrases and heading/definition matches; group results by study with counts and a few representative passages. Give companion results a descriptive title and parent study. Add a small editor-reviewed transliteration/alias map and useful no-result guidance; show any expansion explicitly and retain a literal-only option. Hide redundant single-choice filters. | A curated set of 20–30 definition, phrase, section and spelling-variant tasks finds the expected passage near the top. Distinct philosophical concepts remain distinct. Keep partial-failure retry, query URLs and cancellation working. 3–5 days. |
| UX-03 / P1 — Find and protect personal work | Notes/export and offline saving exist. The notebook is a newest-first list with a disabled scope selector, a link to itself and a minimal empty state. It has no text search or per-study filter. Bookmark records are separate from note backups. | Add note/quote search, study filters and useful empty-state instructions linking back to reading. Remove the disabled/redundant controls. Make offline saving easier to find and clearly distinguish a home-screen icon (SITE-01), a bookmark, a downloaded PDF and a saved offline copy. Add deliberate bookmark backup/restore; indicate what note export includes and when a backup was last exported. Preserve local-only defaults. | Find a note in a 100-note sample, reopen its passage, restore notes and bookmarks in a fresh profile, and identify/update an old saved copy. Failed updates preserve existing work; no surprise downloads or uploads. 3–4 days. |
| UX-04 / P1 — Accessibility and sustained reading | The repaired toolbar and many reader controls have regression coverage, but viewport tests do not establish assistive-technology or physical-device usability. A selected passage and an open mobile drawer also compete for limited screen space. | Run the device/task matrix below. Check native text selection, virtual keyboard, focus restoration, stacked viewers, browser Back, 320-pixel reflow, zoom, contrast and reduced motion. Observe 30-minute sessions using contents, source inspection, notes and Listen; fix reproducible blockers before adding more tools. | Recorded results for supported browsers/devices and assistive technology, with no unresolved data-loss or task-blocking issue. Verify focused controls stay visible below sticky UI. 3–5 days plus participant availability. |
| UX-05 / P1 — Contributor and discussion recovery in production | Phase 4 is merged; local fixtures cover drafts, conflicts, receipts and rich preview. Real OAuth/email callbacks and production write/recovery flows are not established by that evidence. Discussion composer recovery across sign-in/reload is still outside Phase 4. | With an authorized test identity, verify login/logout, one proposal or revision, validation feedback, network interruption, Check submission result and account switching. Agree disposable records before creating them. Preserve unsent discussion text and selected passage through sign-in and reload; show who acts next after a correction. | A reviewed production check record: one intended operation produces one result, retained drafts recover, an uncertain outcome cannot cause a blind duplicate, and comment text survives reauthentication. 2–3 days plus review/identity coordination. |
| UX-06 / P1 — Measured performance | Shared reader assets, lazy collection indexes and explicit offline downloads already exist. The saved RUM baseline is still from 30 August, with only 45 views and questionable CLS/INP interpretation. | Capture a new baseline segmented by catalog, large reader, search and portal, and by mobile/desktop. Trace layout shifts and interaction delays. Measure cold/warm search, diagram rendering and opening tools on a modest Android phone. Optimize only identified costs: initial JavaScript, diagram work, result rendering or dashboard requests. | Save sample sizes, date ranges, metric definitions and before/after traces. Target p75 LCP ≤2.5 s, INP ≤200 ms and CLS ≤0.1 when samples support a conclusion; otherwise report insufficient evidence. 2–4 days plus 7–14 days of observation, longer if traffic is sparse. |
| UX-07 / P2 — Reading orientation and source discovery | Contents, source bibliography previews and figure zoom exist. Per-section reading-time estimates, curated argument routes, source excerpts and a public reference-library browser are not delivered. | Pilot optional section-length/time cues and editor-reviewed routes outside the scholarly essay: full argument, comparison and open questions. Improve companion/deck discovery. Add a searchable source library with edition, language, translation status and permitted page/timestamp links or excerpts. Keep document-source metadata distinct from interpretation. | Readers can choose a manageable session, locate a teaching companion and verify one cited claim. Estimates never imply understanding/completion. No repeated scope boilerplate returns. Never invent PDF page mappings or mirror unapproved sources. 5–8 days plus editorial review. |
| SITE-01 / P1 — Installable website | Favicon and `akhand-samaj-apple-touch-icon.png` / `akhand-samaj-app-icon.png` exist. `favicon_link_tags()` does not emit a Web App Manifest. There is no `theme-color` or iOS web-app meta. The offline service worker registers only after Save, which is correct, but a phone user has no supported Add to Home Screen contract. | Add a generated `manifest.webmanifest` (name, 192/512 icons from the existing Akhand Samaj app mark, `start_url` `/Studies/index.html`, `display` `standalone`, theme/background colours matching the reader). Link it from catalog, readers, discussions and the portal through `favicon_link_tags()`. Add `theme-color` and iOS web-app title/capable tags. Style `display-mode: standalone` so browser chrome is not doubled. Short, dismissible catalog/reader help for Android Chrome and iOS Safari Add to Home Screen. Keep Save-explicit offline registration; do not prefetch discussions, the portal, or authenticated APIs. Do not submit store listings. | Android Chrome and iOS Safari can add the catalog to the home screen; opening the icon lands on Studies; existing offline Save still works; print, Find, and deep links still work in the browser. 1–2 days. |
| SITE-02 / P1 — Follow published updates | `Studies/feed.json` is a tested JSON Feed 1.1 of draft/released Edited-on dates. A homepage `Link` header describes it. The catalog, `Studies/README.md`, and readers have no Follow / Recently updated UI. Many mobile feed apps do not speak JSON Feed. | Add `rel="alternate"` on the catalog. Show a compact Recently updated list from `feed.json` (no new API). A Follow updates control copies or opens the feed URL and names ordinary readers (for example NetNewsWire or Feedly). Generate an Atom sibling from the same catalog writer so those readers work; keep JSON Feed as the canonical machine feed. Document both in OpenAPI and `_verify_studies_index.py`. No per-user follow accounts, no push, no polling backend. | A reader can see newest Edited-on studies on the catalog and subscribe in a feed reader without GitHub. Catalog regeneration keeps feed, Atom, sitemap and index checks in sync. 1–2 days. |
| DIS-01 / P2 — Discussion reply mail and reporting | Boards already list, post, reply (`parentId`), hide and delete. There is no mail when someone replies, no report control, and no “threads I posted on” view. Composer recovery across sign-in is UX-05. Session revocation and extra quotas are OPS-01. | After OPS-01, opt-in email when a new visible reply appears under a comment the signed-in address posted, using the existing Resend path. Preferences on the discussion page (default off or explicit consent at magic-link). A report control that notifies `ADMIN_EMAILS` without GitHub. Do not mail on every catalog edit. Do not add Web Push until SITE-01 exists and reply mail is used. Keep unauthenticated comment reads. | A test identity receives one reply mail and can disable it; a reported comment reaches admins; magic-link quotas and session invalidation from OPS-01 still hold. 3–5 days after OPS-01. |
| FBK-01 / P2 — Lower-friction corrections | Reader Suggest a correction already prefills the GitHub `study-feedback` template (#407). Contribute copy states that a GitHub account is required. There is no first-party issues API. | Keep GitHub as the default: ensure reader and discussion entry points pass study and passage; make the GitHub requirement obvious on a phone. Build a first-party correction form only if UX-04/UX-05 observation shows GitHub is the blocker: Turnstile, bounded size, Worker-created `study-feedback` issue, no `public_repo` from the reader. Do not merge corrections into discussion comments. | Either observation shows GitHub is acceptable, or a reader without GitHub can file one correction that appears as a labeled issue with passage context. 0 days if skipped; 2–4 days if built. |
| OPS-01 / P1 — Dependency and security follow-through | The current Scripts audit still exposes the installer advisory. Discussion sessions lack individual server-side revocation; existing authentication/origin/Turnstile and edge defenses do not close every abuse or retention question. | Upgrade Puppeteer and its pinned Chrome together in a dedicated PR, with compatibility and reproducibility checks. Define discussion session revocation, application quotas, reporting/escalation, retention and cleanup; build on existing admin hide/delete controls. Treat this as a prerequisite for DIS-01. | The specific advisory is resolved; representative Draft/Released/math/diagram PDFs and presentation contracts pass with reviewed pagination changes. Session invalidation and quota/replay behavior have tests; retention and moderation responsibilities are documented. Toolchain: 2–3 days; discussion hardening: 3–5 days. |

### Recommended sequence

1. **Next delivery: finish the reader journey and replace the app idea on the site.** Start with UX-01, search ranking/grouping from UX-02, and notebook navigation/empty states from UX-03. In the same window, ship SITE-01 (manifest / Add to Home Screen) and SITE-02 (Recently updated + feed/Atom). Carry the repaired toggle, Android selection and bookmark targeting as regression checks. Ship in small reviewed PRs; do not reopen the completed contributor implementation phase; do not start a store app.
2. **Run quality checks alongside that delivery.** Start UX-04, UX-05 and UX-06, plus the independent OPS-01 toolchain update. Field sampling and reader recruitment should begin early. Include iOS Safari Add to Home Screen and feed-subscribe in that sampling. Fix failures before treating the release as validated across browsers.
3. **Then discussion engagement and depth of study.** After OPS-01, DIS-01 (reply mail and report). Complete backup/offline discoverability and alias work, then UX-07. Take FBK-01 only if GitHub correction friction is observed. Select among these using observed failed tasks rather than the number of features available.

### Practical evaluation matrix

| Journey | Test context | Record before closing the item |
| --- | --- | --- |
| Start here → read → return; search → passage → Back | Desktop Chrome/Firefox/Safari where available; Android Chrome and iOS Safari | Correct stage/card/query and passage restored; no repeated setup; focus visible. |
| Select → note/bookmark → leave → resume; citation/table → close | Physical phone portrait/landscape and 30-minute reading sessions | Same passage retained; no selection loss, lost note or unexpected scroll jump. |
| Listen → pause → resume → stop | User's working Android Chrome setup plus another available device/voice | Audible playback and truthful status; documented sentence restart; graceful no-voice behavior. The existing user confirmation covers one setup only. |
| Keyboard and assistive technology | Keyboard-only plus TalkBack, VoiceOver or NVDA with their supported browsers; zoom/reflow and reduced motion | Named controls, sensible focus order, announcements, operable dialogs/tabs and no obscured focus. Track individual failures, not a blanket accessibility claim. |
| Backup → restore; save offline → update/interruption | Fresh test browser profile, denied storage/quota simulation, actual offline navigation | Original notes/bookmarks retained, saved-copy state understandable, old working offline bundle survives failed update. |
| Draft → submit/revise → recover | Authorized test identity, controlled network interruption, second tab/account | Recovery and duplicate-prevention behavior verified against production; no real contributor data used as test material. |
| Add to Home Screen → reopen catalog → open a saved study | Android Chrome and iOS Safari | Icon opens Studies; theme and start URL are correct in standalone display; Save-for-offline still works; discussions/portal are not silently cached. |
| Follow updates → open newest study | Catalog Recently updated plus one external feed reader (JSON Feed or Atom) | Newest Edited-on draft/released study matches `feed.json`; ongoing rows stay omitted; no GitHub required. |
| Comment → magic-link → return; later reply mail | Authorized discussion test identity | Unsent text and passage context survive sign-in (UX-05); one opt-in reply mail arrives and can be disabled (DIS-01). |

Observe an initial five readers, including someone new to the terminology and someone using assistive technology where possible. Record task completion, assistance required, wrong turns and time to find a passage. Any lost work is a blocker. This is formative feedback, not a statistical success-rate claim. Do not put note text, selected passages, contributor content, account identifiers or raw search queries into analytics; use aggregate timings/counts or consented test observations.

### Optional work — not committed for the next cycle

- **Native Android/iOS store applications:** out of scope. Revisit only if SITE-01, SITE-02, UX-04 and DIS-01 are shipped and observed readers still will not use the website. Even then start with a read-only client that opens Discuss / Propose / Suggest a correction in the site, not a second writer stack. Do not add agent OAuth or bearer write tokens in order to “support an app.”
- **Web Push:** consider only after SITE-01 and DIS-01 email are in use. iOS push still needs the home-screen install. Prefer email first; it works without a PWA.
- **Phase 6 hybrid/semantic retrieval:** start only when the lexical task set demonstrates unmet conceptual or multilingual needs. Compare retrieval on 50–100 reviewed questions before adding generated answers. No embedding of private notes/submissions; no vector database for every PDF/HTML copy.
- **Cross-device note/bookmark sync:** evaluate demand first. It adds identity, consent, conflict resolution, export/deletion and operating responsibilities to a currently account-free reading tool.
- **EPUB and extended audio:** consider EPUB export, whole-section playback or a curated pronunciation lexicon after real reading tests. Keep device speech limitations explicit; continuous/background listening is not currently promised.
- **Reading-layout experiments:** evaluate a screen-only left-alignment preference if readers struggle with justified prose. The current repository rule requires justified paragraphs on screen and in print; changing that contract would require an explicit policy decision and synchronized rule updates. This review does not change it.
- **Per-user study watch lists:** not needed while SITE-02 (shared feed + catalog Recently updated) covers “what changed.” A signed-in watch list is a new identity product; do not add it for the next cycle.

## Long-study reading experience

These are continuing design principles. Essentials already delivered are recorded below; use the table above to distinguish remaining work from existing tools.

On desktop, use a collapsible contents column, a readable central text column, and an optional sources/notes panel. Expand only the current section’s subsections. On mobile, use drawers for contents and tools, preserving room for the text. Keep browser Back, Find, selection, copy, deep links, and print working normally.

1. **Orientation and resumption:** show the current section, position within it, and an approximate section reading time. Save a stable passage identifier plus offset, rather than only a percentage. Offer an explicit Resume action and named bookmarks without requiring an account. Scrolling must not be described as understanding or completing an argument.
2. **Comfort:** adjustable font size, line height and width; light, dark and sepia preferences. Screen paragraph alignment should be evaluated with readers; keep the existing print contract separate. Controls need visible focus and adequate touch targets.
3. **Finding material:** in-study search with excerpts, match counts and next/previous results; cross-study search with document, source type, language and status filters. Preserve exact Sanskrit/Hindi terms and allow transliteration aliases.
4. **Verification:** open citations beside the passage, showing source title, edition, language, page or timestamp, and permitted excerpts. Provide Copy citation and Copy passage link. Let users enlarge diagrams and tables without losing their place.
5. **Active study:** highlights, private notes, a collected-notes view and Markdown/JSON export. Tie annotations to a document version and warn when their passage changes. Local storage can be the default; sync should be optional and explicit.
6. **Alternative access:** offline reading or EPUB, selected-text read-aloud with sentence highlighting and pronunciation support, and accessible descriptions of diagrams. These are optional tools, loaded on demand.
7. **Reading routes:** add editor-reviewed guides outside the scholarly essay: prerequisites, full argument, comparison, and open problems. Summaries must preserve the distinction between the project’s interpretation and primary texts. Do not collapse satta, Brahman, physical space, or other traditions into convenient but misleading synonyms.

## My Submissions and discussions

Phase 4 now provides separate account/study/artifact/revision drafts, visible save and error state, recovery copies, shared-device cleanup and richer isolated previews. Preserve these contracts while improving their discoverability. Preview approximates common production Markdown/math/diagram structure; final glossary enrichment and PDF pagination still require CI artifacts.

Submission cards now separate publication status (ongoing/draft/released) from workflow status and identify who acts next. Review failures, source comparison, bounded retries, concurrency checks and submission receipts are implemented. Remaining contributor work is production validation and refinement from actual use, not another draft-storage rewrite.

Explain that GitHub is used for contributions and email sign-in for discussions; retain the pending action across authentication. Make public submission visibility and notification preferences clear. Give realistic response expectations instead of implying instant review.

Allow discussions to quote and link a passage (UX-01). Preserve unsent comment text across sign-in (UX-05). DIS-01 adds opt-in reply mail and a report control after OPS-01. Further security work remains in OPS-01: discussion-session revocation, per-user/IP quotas, replay/abuse monitoring, and a reviewed retention policy. Do not treat discussion comments as the structured-correction channel; that stays GitHub `study-feedback` unless FBK-01 is justified.

## Other pages, accessibility and performance

- Keep navigation, theme and terminology consistent across catalog, reader, contribution and reference pages. Make teaching decks and presenter companions easier to discover.
- Build a curated reference library with edition, language, translation status and canonical publisher links. Clearly label working translations and unreviewed transcripts.
- Maintain existing canonical URLs, sitemap, feeds and metadata. Give the API a practical quickstart before protocol detail. Surface `Studies/feed.json` (and any Atom sibling from SITE-02) on the catalog, not only in discovery headers. Keep the installable-site manifest generated and verified with other catalog artifacts.
- Measure catalog, reader and portal separately, on mobile and desktop. Target p75 LCP ≤2.5 s, INP ≤200 ms and CLS ≤0.1 with adequate samples. Audit the surprising stored CLS result before assigning a cause.
- Pre-render diagrams where practical; align browser and PDF Mermaid versions. Give images dimensions, defer below-fold images, and load optional tools only when opened. Extract stable shared assets with content hashes when measurement justifies it.
- Avoid redundant catalog reconstruction and unnecessary dashboard refreshes. Consider incremental/webhook updates and bounded backoff. Keep public comment loading independent of optional authentication work.
- Test WCAG 2.2 AA, including keyboard-only operation, visible focus, labels, contrast, reduced motion and 320 CSS-pixel reflow. Prefer 44-pixel primary touch controls; WCAG’s 24-pixel minimum has defined exceptions. Tables and diagrams may scroll within their own regions.

## Do we need a vector database?

**No vector database is required for the delivered reader or the next UX cycle.** Phase 3 already provides a static, sharded full-text passage index. Improve its ranking, grouping, labels and spelling support before replacing it or adding another service. Exact terms, names, section references and quotations should not depend on an embedding model.

A vector index becomes useful for conceptual or multilingual questions that use different words from the text, related-passage discovery, and retrieval for a cited answer assistant. It is an additional search index, not the document store, a truth checker, or a replacement for lexical search. Use hybrid lexical/vector retrieval and evaluate whether it improves real study tasks.

For the present Cloudflare architecture, a Vectorize pilot avoids introducing another hosting platform. PostgreSQL with pgvector and full-text search is attractive if relational accounts, synchronized notes and other backend needs independently justify PostgreSQL. Thirteen available studies do not justify a distributed data platform by themselves.

Corpus pipeline:

1. Index canonical Markdown once; do not index Markdown, HTML and PDF copies as separate evidence. Resolve references through the rights/storage manifest. Exclude private submissions, private notes and unapproved material from the public index.
2. Chunk by headings and coherent argument boundaries. Experiment with roughly 300–800 tokens, keeping citations and definitions together. Preserve tables and adjacent context where needed.
3. Store document/version, section and passage IDs, title, language, source type, translation status, canonical URL, page or timestamp, and provenance. Distinguish primary texts, project studies, translations and unreviewed ASR.
4. Update incrementally from source hashes and remove superseded/deleted chunks. Apply authorization before retrieval if private collections are later introduced.
5. Combine lexical and vector candidates, optionally rerank, then show the actual passages. Keep generated explanation visually distinct from quotation. Treat retrieved text as data, never instructions.
6. Evaluate 50–100 expert-reviewed questions, including ambiguous, multilingual and unanswerable cases. Measure retrieval relevance, citation support, faithfulness, abstention and task success. Track embedding, query and answer-generation costs separately.

## Validation and rollout

Retain the delivered Phase 1 regressions for mismatched/expired OAuth state, PKCE exchange, missing session storage, hostile request origins/content types, cache headers, repeated/expired magic links, active HTML/SVG, and the date parser. Rebuild generated readers when their source changes; web-only controls do not refresh scholarly timestamps. Verify representative PDFs when rendering inputs change. Keep generated PDFs out of Git and avoid unrelated blanket regeneration.

Ship further changes through reviewed PRs. Coordinate Worker/static deployment when their contracts change; do not redeploy unrelated services for a documentation or screen-only change. Confirm required bindings/migrations and test real GitHub login/logout and email sign-in with an authorized test identity. Do not describe local mocks or successful deployment as production authentication verification.

For later UX phases, observe users finding a definition, comparing an argument, checking a source, resuming a study, and revising a submission. Measure task completion, time, search failures, resumed reading and draft recovery. Use those results to choose the next tools.

## Sources

- [GitHub OAuth authorization and PKCE](https://docs.github.com/en/apps/oauth-apps/building-oauth-apps/authorizing-oauth-apps)
- [OWASP OAuth guidance](https://cheatsheetseries.owasp.org/cheatsheets/OAuth2_Cheat_Sheet.html)
- [OWASP CSRF prevention](https://cheatsheetseries.owasp.org/cheatsheets/Cross-Site_Request_Forgery_Prevention_Cheat_Sheet.html)
- [OWASP DOM XSS prevention](https://cheatsheetseries.owasp.org/cheatsheets/DOM_based_XSS_Prevention_Cheat_Sheet.html)
- [WCAG reflow](https://www.w3.org/WAI/WCAG22/Understanding/reflow.html), [focus not obscured](https://www.w3.org/WAI/WCAG22/Understanding/focus-not-obscured-minimum.html) and [target size](https://www.w3.org/WAI/WCAG22/Understanding/target-size-minimum.html)
- [Core Web Vitals thresholds](https://web.dev/articles/defining-core-web-vitals-thresholds)
- [Pagefind](https://pagefind.app/), [Vectorize metadata filters](https://developers.cloudflare.com/vectorize/reference/metadata-filtering/), and [pgvector hybrid search](https://github.com/pgvector/pgvector#hybrid-search)

## Phase 1 implementation record

Merged in [PR #396](https://github.com/raghavamohan/AnalyticMadhyasthDarshan/pull/396), 5 September 2026. The implementation is delivered; the production-authentication validation limit below remains open.

- Signed, expiring OAuth state is compared before contacting GitHub, with S256 PKCE on authorization and exchange. GitHub tokens remain in required KV storage. Readable legacy session cookies are rejected.
- Both API Workers enforce trusted Origin and JSON on browser writes and apply private/no-store headers to every response. The authenticated machine notification endpoint retains its separate secret-based contract. My Submissions supplies JSON content type for bodyless writes too.
- Discussion magic links are stored as SHA-256 digests and consumed by one conditional SQL write. Existing D1 tables suffice; pre-change links require replacement.
- Author HTML passes a pinned HTML5 sanitizer before trusted reader chrome and KaTeX rendering. Static SVG validation rejects active content and external resources; metadata cannot close its JSON-LD script. Mermaid is pinned consistently to 11.17.2 in browser and PDF and uses strict security mode.
- PDF loading blocks network requests and unrelated local files. Browser scripts are disabled during initial document loading; the bundled strict diagram renderer runs afterwards. Chrome receives OS/locale variables instead of publishing credentials. This narrows the document-rendering boundary; it does not turn arbitrary contributor-modified CI scripts into trusted code.
- Reader Next works above the first section; section navigation records a link and moves focus; subsection and main-content anchors account for the toolbar. Light/dark canvas colors are explicit and diagrams redraw on theme changes. Long words and inline equations no longer widen the mobile page; mobile toolbar controls have 44-pixel heights.
- Catalog dates use an explicit IST parser for ordering and document version URLs. The 45 existing study/companion readers were rebuilt, and the full publishing pipeline produced one additional companion reader from its existing Markdown; no canonical study Markdown, status or Edited-on timestamp was changed.
- API CI tests and bundles both Workers, including changes in their shared request guard. Deployment is restricted to master. PDF build-selection rules include the new rendering-security helpers.
- Updated vulnerable Mermaid and compatible transitive dependencies, including both Workers' Wrangler tooling. Both Worker dependency audits report zero known vulnerabilities at validation time.

Validation completed locally: all 31 published Markdown-derived PDFs rebuilt and verified; all 36 enforced script suites; both Workers' real route tests and Wrangler dry-run bundles; catalog and agent-rule synchronization; static SVG validation; repeated byte-identical PDFs for a Draft, a Released study, and the formal study with math and eight diagrams. Browser checks cover 320/390-pixel reader reflow, theme selection, initial Next, subsection anchor clearance, and catalog date/title sorting. Production GitHub/email authentication still needs a post-deployment check with an authorized test identity.

Follow-up limits remain explicit: KV logout propagation is not instantaneous across locations; discussion cookies do not yet have per-session server-side revocation; further abuse controls and retention policy remain future work. The reader workspace, resume/bookmarks, passage search and independent draft recovery have since shipped in Phases 2–5. Vector retrieval remains optional Phase 6.

**Open dependency follow-up, rechecked 7 September:** the Scripts audit still reports four high-severity package entries, all from the same `extract-zip` advisory through Puppeteer's browser installer. A malicious browser ZIP can contain unsafe symlinks. This is a build-tool dependency, and the document renderer does not accept ZIP archives; nevertheless, the dependency remains vulnerable. Keep browser acquisition restricted to the configured trusted source. OPS-01 schedules a dedicated Puppeteer/toolchain upgrade, with Node compatibility, the explicit Chrome pin, PDF reproducibility, pagination, math, diagrams and presentation tests verified together. The audit suggests Puppeteer 25.10.0, a major upgrade; the current contract retains Puppeteer 24.43.1 and Chrome 148.0.7778.97. See the [extract-zip advisory](https://github.com/advisories/GHSA-jmr9-qjv8-65gv). This is a recorded residual risk, not an audit-clean claim.

Sanitizer implementation follows the [nh3 HTML5 sanitizer documentation](https://nh3.readthedocs.io/en/latest/).

## Phase 2 implementation record

Merged in [PR #400](https://github.com/raghavamohan/AnalyticMadhyasthDarshan/pull/400), 6 September 2026.

- **Contents beside the text:** a collapsible desktop panel follows the current heading and expands its section's subsections. Previous/Next and the current heading remain in the toolbar. The original small-screen modal was replaced in #409 by a drawer below the interactive toolbar, with keyboard tabs, Escape and focus restoration.
- **Reading comfort:** five text sizes, three line spacings, three column widths, and device/light/sepia/dark colors. Preferences survive reload and apply across study readers. Changing the type size or opening the desktop panel keeps the same passage in view.
- **Resume a passage:** save a paragraph identifier, its section, an excerpt and the position within the paragraph. An explicit Resume banner appears on return; a supplied deep link takes precedence. If text changes, recovery uses an unambiguous excerpt in the original section or opens that section with a notice. A missing passage is reported instead of guessing.
- **Named bookmarks:** add, revisit, rename and remove up to 100 places per document. Names and excerpts render as text. Autosave reads the latest stored bookmark collection, and other tabs receive updates. Confirmed cleanup removes only the current document's saved places.
- **Local storage:** no account or new service is required. Storage failures retain changes for the visit and explain that they cannot persist. Unreadable saved data is preserved until the reader deliberately clears it. These are device-local conveniences; clearing browser data removes them. Note export/restore shipped in Phase 5; bookmark backup is UX-03, and cross-device sync remains optional.
- **Shared delivery:** screen CSS and deferred JavaScript are separate reusable assets, versioned by their contents. All 46 tracked study and companion readers were regenerated. An enforced check catches stale asset references. These two screen-only assets do not invalidate PDF caches; changes to the markup helper still trigger the PDF verification workflows.

Validation: 31 published Markdown PDFs rebuilt through the internal pipeline. Draft, Released and formal/math/diagram samples remained byte-identical to their pre-change PDFs. Automated checks cover preference and stored-data validation, passage recovery after edits, ambiguous matches, navigation boundaries, control wiring, print isolation, asset synchronization and cache behavior. Chromium browser checks cover desktop and 320/390-pixel layouts, the largest text setting, theme persistence, section links, browser Back, explicit resume, bookmark renaming, cross-tab changes, keyboard drawer operation, unavailable/full storage, unreadable-data recovery and hostile bookmark labels.

Before calling this a complete accessibility or usability evaluation, test with screen-reader users and readers using Safari/Firefox and real mobile devices. Observe whether readers can resume and find an argument without assistance. Phase 3 subsequently delivered passage search, citation previews and enlarged diagrams/tables; a vector database remains unnecessary for these reader essentials.

## Phase 3 implementation record

Merged in [PR #401](https://github.com/raghavamohan/AnalyticMadhyasthDarshan/pull/401), 6 September 2026.

- **Find an argument:** the reader's Find tab searches words or quoted phrases within the open document. Results include the section and a highlighted excerpt; Previous/Next move between matching passages. Modern browsers highlight the matching words without replacing the document's text or breaking definitions, citations and equations. A passage outline remains available when word highlighting is unsupported.
- **Search the collection:** `Studies/search.html`, linked from Browse all studies and the reader, covers 13 published studies and 18 companion documents. Filter by document, study/note, status and language. All query words must occur in one passage. Latin diacritics are ignored; Hindi vowel signs and spelling are preserved. This is literal search, without inferred synonyms, translation or generated answers. Equation text is indexed once; raw Mermaid syntax is excluded.
- **Load only when needed:** opening a reader downloads no collection index. Collection search first loads a roughly 12 KB manifest, then fetches separate indexes for selected documents with four concurrent requests. Successful indexes are reused during the visit. The complete corpus is about 3 MB of JSON, about 0.85 MB with gzip; these are local artifact measurements, not production transfer or latency guarantees. Results are shown in batches. Aborted searches cannot replace newer results, and partial failures identify unavailable documents and permit retry.
- **Share a passage:** anchors exist in generated HTML before JavaScript and preserve Phase 2 bookmark IDs. Link & sources copies a canonical passage URL with its original section and source version. Unrelated edits leave the passage ID intact. Changed text produces a new ID; an obsolete link opens the original section with a notice when possible. This is recovery, not an archive of old versions.
- **Check a citation:** recognized source codes and bibliography links open the actual bibliography entry beside the selected passage. Readers can search the study's references, copy a citation, or open the source in a new tab. The preview does not fetch or summarize a source PDF and does not infer PDF page numbers from printed citations. Ambiguous source codes are not assigned automatically. Clipboard failures offer selectable text.
- **Read figures and tables:** Enlarge opens a modal viewer with zoom, Fit, 100% and scroll/swipe controls. Tables start at readable size, including on phones. Diagram IDs and internal references are isolated from the document. Closing the viewer restores the reading position and keyboard focus.
- **Keep publication consistent:** all 46 existing readers were regenerated. The converter updates only the changed document's index; catalog writes remove obsolete/unpublished entries. Verification rejects stale source versions, missing indexes, duplicate/mismatched inventory and stale generated search pages. Indexes contain public documents only. No account, search API, vector database or new hosting service is required. Screen asset and search-index edits preserve PDF-cache reuse; shared renderer changes still run PDF checks.

Validation includes all 31 published Markdown PDFs through the internal renderer, all 39 enforced script suites, the six additional reader-layout checks, reference integrity, catalog/search synchronization and agent-rule synchronization. Draft and Released text samples remain byte-identical to Phase 2. The 84-page formal study has a different PDF hash after adding diagram passage IDs, but every page's text and rendered pixels and the complete outline match the Phase 2 PDF; two repeat builds produce identical new bytes. Canonical study Markdown, scholarly timestamps and statuses are unchanged.

Chromium checks cover desktop and 320/390-pixel layouts, quoted queries, filters and result pagination, matching-passage clearance below the toolbar, source metadata and copying, focus restoration, enlarged Mermaid/table rendering, and partial index failure/retry. Real-device, Firefox/Safari and screen-reader usability checks remain necessary before claiming comprehensive accessibility or field performance. The complete reference library and multilingual semantic retrieval remain future work. A vector index is still unnecessary for these literal find-and-verify tasks; evaluate the hybrid-search pilot described above only when conceptual retrieval becomes a demonstrated need.


## Phase 5 implementation record

Merged in [PR #402](https://github.com/raghavamohan/AnalyticMadhyasthDarshan/pull/402), 6 September 2026. Phase 5 was completed before Phase 4 at the maintainer’s request.

- **Active reading:** select prose and choose Highlight, Note or Listen. Four highlight colors and a private note editor retain the quoted text, original section, passage anchors and full Markdown source version. A note can cover several passages. With no selection, the Notes tab offers the current readable passage. Highlights do not replace text nodes or interfere with glossary, citation, search and print markup. Where custom word highlighting is unavailable, the marked passage retains a visible side indicator.
- **Changes and recovery:** an exact quote is recovered only when its passage or section/context match is unambiguous. Updated studies show a version warning. Missing or ambiguous passages keep their original quotation; readers can deliberately reattach a note to a new selection. This is not a historical document archive. Revision checks prevent stale edits or deletions from overwriting another tab's work. Failed saves retain the editor text, and exports include unsaved text.
- **Personal notebook:** `Studies/notebook.html` collects notes across documents and lists saved studies with dates, versions and availability. It is linked from the catalog and study tools. Notes are stored in a separate IndexedDB database in the current browser profile, without accounts, uploads, sync or analytics events. Anyone sharing that profile can read them. These notes are private to the device, not encrypted personal storage.
- **Portable backups:** export Markdown for ordinary reading/editing and JSON for restore. Imports are validated before committing; identical records are skipped and conflicting versions are retained as separate notes. Limits are 200 notes per document, 2,000 overall, and 8 MB of formatted note data; selections are limited to 6,000 characters and 20 passages, note text to 12,000 characters, and imports to 10 MB. If persistent storage is unavailable or unreadable, existing data is preserved and new work can be exported from memory before leaving.
- **Offline studies:** choose Display → Offline reading → Save. Save is explicit and includes only the public reader, notebook and required reading assets, local diagrams/images and math fonts. A public manifest lists every expected byte count and SHA-256 checksum. A staged bundle becomes the saved copy only after every file passes verification; failed, interrupted, oversized or quota-limited updates preserve the previous copy. Concurrent saves are serialized, and abandoned temporary bundles are reclaimed on the next save. Up to 30 documents can be saved, with 20 MB per document and 8 MB per asset. Removing a saved copy leaves notes and bookmarks intact.
- **Clear offline boundaries:** saved HTML shows a saved-copy notice. Document navigation prefers the network; a failed connection uses the saved copy, while an online 404/withdrawal is respected. Opening an unsaved document offline offers the saved-study library. Source PDFs, references linked as separate documents, discussions, contribution pages, authenticated APIs and collection indexes are not downloaded. Browser storage can be evicted or cleared; JSON/Markdown backups are the durable escape route. Save again when the site changes to refresh a copy.
- **Optional audio:** choose an installed device voice and speed, then Read selection, Pause, Resume or Stop. No audio starts automatically. Only voices reported as local are offered; sentence/chunk highlighting tracks playback. Browser/device support and Sanskrit/Hindi/formula pronunciation vary. This is not a recorded audiobook or a pronunciation dictionary. The [voice API documents its local/remote flag](https://developer.mozilla.org/en-US/docs/Web/API/SpeechSynthesisVoice/localService); [browser cache storage has browser-managed lifetime and quotas](https://developer.mozilla.org/en-US/docs/Web/API/Cache).
- **Android audio follow-up, merged #408:** capture native selection changes and preserve the final range before opening the mobile tools dialog; show the selected passage in Listen. Speech initializes independently of notes storage, prefers a local voice matching the study language, and offers a separate Test voice action. Short utterances, start-timeout recovery and guarded callbacks prevent silent startup from looking like active reading. Pause cancels the current utterance; Resume repeats that sentence or short chunk because [Chromium on Android stops on pause and has no native resume implementation](https://chromium.googlesource.com/chromium/src/+/HEAD/content/browser/speech/tts_android.cc). Collapsible help links to Android’s voice setup instructions. Controlled engine tests cover these cases; the user confirmed working playback on their Android Chrome phone after the fix. Other devices, voices and long sessions remain part of UX-04.
- **Toolbar consistency follow-up, merged #409:** the mobile tools panel previously covered its own opener with a modal dialog. It now opens below the toolbar, leaving Contents & tools available to toggle it closed. Escape, the close control and tapping the shaded area also dismiss it; covered study content is temporarily inert while toolbar navigation remains available. Studies, Contents & tools and More share typography, theme colors and hover/focus styling; disclosure arrows and an underline indicate expanded controls. Short landscape screens scroll the complete tools panel. Listen retains its selection across opening and closing the drawer.
- **Static deployment and print:** the service worker is registered only after Save. Its root scope supports Studies and Applications, with explicit URL allowlists and credentials omitted from saved-resource requests. Mermaid's pinned browser bundle is vendored with its license/checksum for offline diagrams; it loads only in diagram documents. PDF rendering continues through the existing internal pipeline. Screen assets and offline manifests do not invalidate PDF build caches; shared generator changes remain covered by the PDF checks. No vector database, new hosting account or backend service is required.

Validation: the 40 enforced suites pass, including the new annotation/offline integrity contracts. A browser fixture exercises 13 real IndexedDB transaction, concurrent-writer, conflict, restore and rollback checks in an isolated test database. All 31 published Markdown PDFs were rebuilt and verified; Draft, Released and the formal math/diagram sample match their Phase 3 PDFs byte-for-byte. Canonical study Markdown, scholarly timestamps, status and passage content are unchanged. All 46 generated reader bodies match the Phase 3 main content exactly. The catalog, search/offline artifacts, six additional layout checks, reference integrity and agent-rule mirrors pass. Chromium checks cover selection, 320/390-pixel editing, colors, cross-tab conflicts, Markdown/JSON export of unsaved work, JSON restore, and device-voice pause/resume/stop. With the preview server stopped, the notebook opens and the formal study renders all eight diagrams and 268 math expressions with its fonts loaded; an unsaved study offers the saved-study library. Offline bundles currently range from about 0.23 to 4.95 MB (uncompressed bytes), and their manifest is about 107 KB.

Follow-up evaluation: test Safari/Firefox, additional real mobile setups, assistive technology, offline eviction/recovery and long reading sessions with users. Browser tests and one Android confirmation do not establish comprehensive accessibility or field-performance results. Consider optional sync and curated pronunciation only after readers demonstrate a need. Phase 4 is also merged; Phase 6 remains an optional evaluated semantic-retrieval pilot rather than a dependency of these study tools.


## Phase 4 implementation record

Merged in [PR #403](https://github.com/raghavamohan/AnalyticMadhyasthDarshan/pull/403), 6 September 2026, after the maintainer authorized the deferred contributor enhancements. Full contributor guidance, deployment details and recovery limits are saved in [Contributor reliability](contributor-reliability.md). UX-05 tracks the remaining production validation.

- **Independent browser drafts:** account, study, artifact filename and PR revision identify separate IndexedDB workspaces. Proposals can be started independently. Visible save status, explicit retry, recovery checkpoints, current-text JSON export/import, retained presentation bytes, draft listings and account-specific cleanup replace the two global localStorage drafts. Older unassigned drafts require deliberate recovery. Saves and deletes use revision checks; another tab cannot overwrite newer saved text. Draft listings read small metadata records rather than loading every attachment.
- **Preserved work:** source/file loads are fenced against changed accounts, contexts and editor text. Browser draft recovery precedes source loading. Failed storage retains the editor and permits a backup download; navigation warns about unsaved changes. Submission receipts persist before a request is sent. Confirmed submission retains a recovery copy. The bounds are 100 drafts and 64 MB including recovery copies, with the existing 2 MB Markdown and 10 MB presentation submission limits.
- **Reviewable edits:** Compare with loaded source displays the changed source region and line counts without executing its text. GitHub blob versions are checked before replacing existing Markdown, and in-place revisions retain GitHub’s optimistic concurrency condition. A source conflict explains how to download, reload, recover and compare. Binary presentation merges and in-place portal editing of every PR type remain outside this change.
- **Richer, isolated preview:** headings, tables, lists, emphasis, quotes, fenced code, KaTeX and Mermaid now render. A sandbox and CSP isolate sanitized author content; external image requests and navigation are disabled. Preview assets are pinned, licensed, lazy-loaded and addressed by their content hashes. Common Markdown structure is compared against the production renderer; final metadata, glossary enrichment and PDF pagination still require CI-artifact review. Preview has explicit size/equation/diagram limits and reports syntax failures without losing source.
- **Actionable dashboard:** workflow and publication status are separated. Each card identifies who acts next. Reviewer feedback and failed-check summaries appear alongside links to full GitHub details. Check aggregation includes current attempts across check names rather than showing a stale failed rerun or ignoring failures outside the study job. Account/request fencing keeps late dashboard and notification responses from replacing a newer account’s state. Reads have one bounded retry; automatic polling pauses when hidden.
- **Duplicate prevention:** a SQLite-backed Durable Object in the existing submission Worker records account-scoped receipts for proposals, submissions and revisions. Atomic claims serialize attempts across devices; identical retries replay results and altered content cannot reuse a receipt. After an uncertain response, Check submission result reconciles GitHub markers or revision commits. A receipt that has not arrived may only be retried with the same ID. Unknown outcomes remain blocked for investigation rather than generating another public submission. The included migration needs no new secret; normal protected-branch deployment provisions it.
- **Clear expectations:** the portal distinguishes GitHub contribution sign-in from discussion email sign-in, states public submission visibility, retains the pending form context through login, explains browser-only storage and exposes shared-device cleanup. Existing notification preferences remain on the dashboard; the interface does not promise a fixed maintainer response time.

Validation: the 41 enforced script suites pass, including contributor draft/receipt/preview tests; both Workers pass their security route and deployment-bundle checks. Chromium’s isolated fixture passes 13 actual IndexedDB transaction, conflict, checkpoint, binary-recovery and cleanup checks. Portal checks cover account/file switching, an approved proposal, revision reload, lost-response recovery, source comparison, malicious preview markup, tables/equations/diagrams, presentation reload, a restored UTF-8 Hindi note submitted to the mock API, and exporting unsaved text when storage is unavailable. Phone viewports at 320 and 390 pixels have no page-level horizontal overflow. Canonical study sources, timestamps, readers, catalogs and PDF generation are unchanged; index verification passes.

Limits: authenticated production submission, the real OAuth callback, field latency, Safari/Firefox and assistive-technology testing are not established by local fixtures. Some interrupted operations require maintainer investigation, particularly a branch written before PR creation or a process crash before a result can be recorded. GitHub and Cloudflare cannot provide a shared atomic transaction; there is no claim of universal automatic recovery or exactly-once delivery. Detailed limits and deployment sequencing are recorded in the linked contributor document. No vector database is introduced.

## Listen convenience follow-up — 7 September 2026

- Listen offers the current prose paragraph without requiring text selection. It uses the same last-click/reading-position source as Bookmarks and displays the section and passage preview. At a heading or figure it offers the next readable paragraph. An explicit text selection takes precedence.
- The selection menu's **Read** action starts the selected text immediately and opens Listen with the playback controls in view. Opening Listen alone remains silent. Read/Pause/Resume/Stop appear before voice and speed settings; the preview stays tied to the active or paused audio even when the reader moves elsewhere.
- Device speech still starts directly from an explicit tap, independently of notes storage. A late voice list does not queue automatic playback. Existing Android chunk/pause behavior is retained. Selections or paragraphs above the 6,000-character limit require selecting a smaller part instead of silently truncating audio.
- Validation covers current/clicked paragraphs, selection precedence, direct playback, frozen playback previews, late voices, storage delays and pause/resume. Browser checks use the generated reader on desktop and phone-sized Chromium viewports; a physical Android follow-up remains appropriate after deployment.
